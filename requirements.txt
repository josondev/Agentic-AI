# Core dependencies - let UV resolve compatible versions
flask>=3.0.0
gunicorn>=21.2.0
python-dotenv>=1.0.0

# LangChain ecosystem - remove strict version pins
langchain>=0.1.0
langchain-core>=0.2.0
langchain-community>=0.0.13
langchain-groq>=0.1.0
langgraph>=0.0.20

# Search and utilities
tavily-python>=0.3.0
wikipedia>=1.4.0
nest-asyncio>=1.5.8

# Skip Ollama for cloud deployment (causes conflicts)
# langchain-ollama - not needed for Render
